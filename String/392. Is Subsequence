class Solution {
public:
    bool isSubsequence(string s, string t) {
        for(int i = 0; i < t.size(); i++){
            if(t[i] == s[0]) s = s.substr(1);
        }
        if(s.size() == 0) return true;
        return false;
    }
};
